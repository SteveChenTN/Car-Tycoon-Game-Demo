# Industrial Empire / AutoMogul 项目接管与进度说明

本文档用于 Codex 接管项目后的开发进度盘点。它的目标是建立一个诚实、清晰、便于继续协作的项目基线，也展示 AI 辅助开发在复杂模拟经营游戏原型中的有效产出。

审计依据：

- `C:\Users\18194\Downloads\Industrial_Empire_GDD.pdf`
- 当前仓库代码：`backend/`、`frontend/`、`assets/data/`
- 本次验证命令：`npm run build`、`npm run lint`、`python -m compileall -q backend`、`python -m pytest backend/test_data_driven.py backend/test_unlocked_components.py -q`、后端路由导入、贷款接口与月报接口 smoke test

> 说明：新版 GDD 中游戏名为《工业帝国》(Industrial Empire)，当前代码与前端文案中仍大量使用 `AutoMogul`。以下将两者视为同一项目在不同开发阶段的命名。

## 总体判断

当前项目已经形成了相当可观的全栈原型基础。后端不仅有 FastAPI、SQLite/SQLAlchemy、多存档和回合推进框架，也已经沉淀了工程物理计算、引擎/底盘/车型设计、生产库存、市场效用模型、财务贷款、事件、世界生成和数据驱动加载等系统。前端也已经具备主菜单、仪表盘、工程、工厂、市场、高管、研发和报表等主要界面雏形。

与新版 GDD 相比，项目下一阶段的重点更适合从“功能原型扩展”转向“系统闭环整合”。新版 GDD 最有辨识度的核心是 AI 竞争对手、AI 高管和委托管理系统；当前代码已经有相关模型和局部决策基础，但还需要进一步接入回合循环、前端交互和长期经营反馈。

整体来看，这个项目很好地体现了 Vibe Coding / AI 辅助开发的优势：可以在较短时间内搭出复杂游戏系统的骨架，并生成大量可继续打磨的业务代码。后续需要做的是工程化收敛、接口统一、测试补齐和玩法闭环。

## 功能状态总览

状态定义：

- `基本完成`：已有可复用的模型、逻辑、API 或 UI，能作为正式系统继续扩展。
- `部分完成`：已有明确代码基础，但仍需要接入主循环、补齐前后端联动或完善关键流程。
- `已设计待开发`：GDD 已定义方向，当前代码中还没有形成可用系统。
- `需与 GDD 对齐`：当前实现与新版 GDD 在命名、接口、架构或玩法重心上存在差异，需要后续统一。

| 模块 | 当前状态 | 说明 |
| --- | --- | --- |
| 游戏会话、存档、时间推进 | 部分完成 | 已有多存档系统、`/api/v1/game/new`、`/load`、`/next_turn` 和周度时间系统；后续可继续对齐 GDD 的 `games/{game_id}` 风格接口，并完善回合阶段结果。 |
| 世界生成与地区经济 | 部分完成 | `WorldGenerator` 可生成地区、玩家公司、AI 公司和基础技术树；地区模型比 GDD 示例更丰富，起始年份配置需要统一。 |
| 公司与财务 | 部分完成 | 公司模型、现金、债务、信用评级、贷款、税务、月报已有基础；本轮已开始把生产、研发、营销、贷款利息、销售收入写入公司账本和 `FinancialHistory`，股东、融资证券化、股息、回购、M&A 与反收购仍是后续扩展方向。 |
| 工程与车型设计 | 基本完成 | 引擎、底盘、车辆配置、物理计算、技术门槛、熟悉度、逆向工程、研发项目已有较深实现，整体深度超过 GDD 示例；后续重点是车型上市、改款、停产、生命周期和销售联动。 |
| 研发与技术树 | 部分完成 | 技术树数据、研发项目管理和前端 TechTree 已存在；项目完成、技术解锁和公司战略联动还需要继续完善。 |
| 生产、工厂、库存 | 部分完成 | 工厂、生产线、库存、材料采购、零部件生产、整车装配、换线重整、生产熟悉度均有实现；主回合生产结算、工厂建设、供应合同入库和周度产量闭环是下一步重点。 |
| 市场求解与销售 | 部分完成 | 后端存在 Multinomial Logit 市场模拟、消费者桶、品牌认知、二手车模型；新车销售已开始写入公司收入、成本和销量，市场结果到完整库存、销售历史与报表的链路仍需继续打磨。 |
| AI 竞争对手系统 | 部分完成 | 有 AI 公司原型、CEO 个性和决策引擎；GDD 五维矩阵中的 `远见(Foresight)`、组织惯性，以及 AI 决策执行器的生产/定价/研发/扩张落地仍需补齐。 |
| 组织惯性系统 | 已设计待开发 | GDD 规划了公司规模越大决策周期越长的机制，当前还没有形成独立系统。 |
| CEO 变更机制 | 已设计待开发 | 年龄退休、绩效解雇、董事会冲突、被挖角等机制尚待实现。 |
| AI 高管系统 | 部分完成 | `Staff`、`HRSystem` 支持高管属性、招聘、解雇、士气、忠诚度、退休和跳槽风险；KPI、管理哲学、自然语言委托和高管报告还需形成闭环。 |
| 自然语言指令 | 部分完成 | 有 `DirectiveParser` 和 `Directive` 模型，可识别设计、生产、营销、研发、HR、财务类文本；正式 API、UI 和执行管线仍需接入。 |
| 事件系统 | 部分完成 | `GameEvent`、`EventTemplate`、新闻生成和基础效果处理已存在；小/中/大事件层级、历史危机和长期影响可以继续扩展。 |
| 品牌系统 | 部分完成 | 公司声望、质量/创新声誉、地区品牌认知五维向量已存在；品牌与召回、赛事、营销、销量、用户满意度的长期反馈可以继续加强。 |
| 员工与工会 | 已设计待开发 | 当前已有员工数量、士气和高管/员工模型；工会、工资谈判、罢工和工作条件系统尚待开发。 |
| 供应链危机 | 部分完成 | 供应商、合同、B2B、材料市场和 AI 采购有基础；自然灾害、地缘政治、技术瓶颈、供应商破产等危机链条尚待扩展。 |
| 法规、合规、召回 | 部分完成 | 有法规、车辆合规、召回事件模型和克隆底盘法律风险服务；合规审核、禁售、召回执行和品牌影响还需接入主循环。 |
| 历史回放 | 部分完成 | 有销售/财务/二手车历史表和 API，月报 UI 已存在；时间线视图、里程碑、车型生命周期回放可作为后续体验增强。 |
| 前端主框架 | 部分完成 | React/Vite/Tailwind、主菜单、暂停菜单、左侧导航、Dashboard、工程、工厂、市场、高管、研发、报表页面均存在；当前前端生产构建已恢复通过，后续重点转为交互验收、数据闭环和界面一致性。 |
| Electron 桌面壳 | 已设计待开发 | GDD 规划 `electron/`，当前仓库尚未形成可交付桌面壳。 |
| 数据驱动与 Mod | 基本完成 | `assets/data`、数据加载器、组件/物理/技术/事件 JSON 和 `MODDING_GUIDE.md` 已存在，是当前项目较稳的基础之一。 |

## 与新版 GDD 需要对齐的地方

1. 命名：GDD 使用《工业帝国》(Industrial Empire)，当前应用名、日志和前端仍是 `AutoMogul`。
2. 架构：GDD 规划 `backend/app/`、`services/ai/`、`api/v1/`、`shared/`、`electron/`；当前代码采用 `backend/core`、`backend/logic`、`backend/api/routes`，需要决定是保留现状还是逐步迁移。
3. API：GDD 使用以 `games/{game_id}` 为根的 REST 风格；当前接口更偏扁平化，如 `/api/v1/game`、`/api/v1/company/{company_id}`、`/api/v1/engineering`。
4. 状态管理：GDD 提到 Zustand，当前前端使用 Context API。现阶段可以先保持 Context，等状态复杂度继续上升时再评估迁移。
5. 玩法重心：当前最成熟的是工程/生产细节；新版 GDD 最强调的 AI 竞争者和 AI 高管委托系统，需要作为后续差异化重点。
6. 时间单位：GDD 明确以周为单位，代码中已有周度时间，但市场、生产、财务部分逻辑仍带有月度语义，需要统一结算口径。
7. 数据单位：公司现金、贷款、成本等在部分代码中混用“百万游戏币”和原始金额，后续应制定统一单位规范。

### API 兼容层状态

- Future-facing 路由已统一放在 `/api/v1/games` 和 `/api/v1/games/{game_id}/...` 下。
- Legacy 路由如 `/api/v1/game`、`/api/v1/engineering`、`/api/v1/company`、`/api/v1/debug`、`/api/reports` 继续短期保留。
- Swagger 会把 legacy HTTP 路由标记为 deprecated，并将 future-facing 路由单独分组。
- 前端服务层应通过 `frontend/src/services/apiClient.ts` 生成 API 路径，避免继续硬编码混杂的 host/path。

## 当前接管进展

### P0 稳定化已完成一轮

本轮主要修复目标是让项目从“可继续开发的原型”回到“可验证、可构建、接口不明显错位”的状态。当前结果如下：

- 前端生产构建已通过：`npm run build` 可以完成 TypeScript 检查与 Vite 打包。
- 前端 lint 已可执行且无阻断错误；当前剩余的是 React Hook 依赖、`any` 类型和 Fast Refresh 约束等警告，适合在后续质量整理中分批处理。
- 后端固定到 `.venv` / Python 3.12 的测试入口，`backend/TESTING.md`、`pytest.ini` 和 `backend/conftest.py` 已补齐。
- 后端基础验收测试已通过：数据加载、组件解锁等 8 个测试通过，并使用隔离 SQLite 测试库，避免污染真实存档。
- `backend/api/routes/game.py` 已能编译通过；之前提到的 `line 197` 语法/编码损坏在当前工作区没有复现。
- `GameContext` 与 `gameApi` 已补齐当前存档、`game_id`、`player_company_id` 和玩家公司摘要的兼容处理，前端页面不再依赖固定 `companyId = 1`。
- Staff / Executive 相关接口已改用当前 `Staff` 模型字段，招聘、解雇、候选人列表保留在统一的 `/api/v1/staff/*` 路由下。
- Company / Debug 路由中的公司财务、声誉、员工数量和贷款字段已与当前模型对齐；贷款申请和提前还款接口也修正了枚举、参数名与返回结构。
- P1 核心经营闭环与 AI 竞争对手修复已完成本轮质量闸：生产、采购、研发、营销、贷款利息和市场销售已进入公司月度账本；库存、销售历史、需求历史、财务快照和月报 API 已形成可测链路。

### P1 本轮修复状态

本轮 P1 修复重点是把“系统原型”推进到“可通过自动化验收的经营闭环”。当前结果如下：

- 周度生产已接入回合阶段：生产线会按周产能产出零部件和整车，材料不足会阻断产出并生成生产事件，供应商合同交付会补充材料库存。
- 市场销售会改变游戏状态：新车销售会扣减经销商库存，写入公司收入、制造成本、销量、`SalesHistory` 和 `MarketDemandHistory`。
- 财务报表闭环已补强：`FinancialHistory` 记录现金、收入、成本、库存价值、资产负债和市场份额快照，报表页与月报接口读取真实快照。
- AI 竞争对手进入可执行闭环：AI 决策基于现金、利润、市场份额、品牌健康和产能利用率等真实指标；可执行营销、定价、研发、扩张等动作，并写入事件日志。
- CEO 五维人格与组织惯性已落地：`foresight` 已加入人格矩阵，AI 决策会进入队列并按公司规模延迟执行，远见会影响技术布局时机。
- GDD 风格 API 兼容层已完成第一版：`/api/v1/games/{game_id}/...` future-facing 路由可用，legacy 路由在 Swagger 中标记为 deprecated。
- 本轮验收结果：`python -m pytest -q` 为 25 passed，`python -m compileall -q backend` 通过，`npm run build` 通过，`npm run lint` 为 0 errors / 42 warnings。

### 已知温和 caveat

`backend.main` 已能导入并注册路由；在 Windows GBK 控制台里，日志中的特殊符号会触发一次非阻断的编码告警。它不影响导入结果，也不同于此前的后端语法崩溃。后续可以把日志符号替换为 ASCII，或统一服务启动时的 UTF-8 输出环境。

完整的“启动后端 + 启动前端 + 真实存档点击进入主界面”仍建议在下一轮做一次浏览器端验收，尤其是针对真实存档数据和长流程交互。当前 P0 已经覆盖构建、导入、基础后端测试和关键 API smoke test。

另外，`backend/test_scripts` 与 `backend/scripts/demo_*` 中仍保留少量旧字段或硬编码演示数据；这些脚本不在默认 pytest 与运行时主路径内，建议后续作为文档/演示脚本整理项处理。

## 后续方向摘要

1. `P0` 稳定守护：保持前端构建、后端测试和关键 API smoke test 绿灯，并补做真实浏览器存档进入主界面的长流程验收。
2. `P1` 守护与打磨：保住生产、市场、财务、报表和 AI 决策测试绿灯，并补真实存档浏览器长流程验收。
3. `P2` AI 高管委托：补齐 KPI、管理哲学、自然语言指令、报告和自动执行。
4. `P2` 市场与品牌：强化消费者效用、区域偏好、营销、品牌认知与销量反馈解释。
5. `P3` 深层经营：扩展供应链危机、工会、法规合规、召回、M&A、股东与融资。
6. `P4` 体验完善：补齐历史回放、任务模式、Electron 打包、教程与本地化。
